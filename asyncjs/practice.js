function fetchCustom(urlb ){
    console.log("starting downloading data",url);
    setTimeout(function process(){
        console.log("download complete");
        let response = "dummy data"

        
    },3000);
}